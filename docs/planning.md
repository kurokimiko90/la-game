# 《記憶小鎮》沉浸式語言找物遊戲 — 規劃書 v0.3

> 暫定名：記憶小鎮（Memory Town）｜更新：2026-09-22｜狀態：草案；M1 原型已做完，但還沒試玩；新場景改為自動擴展
> 標記「⚠️ 假設」的內容尚未調研或確認，見第 8 節待確認清單。
> 開發說明見專案根目錄 `CLAUDE.md`；場景製作標準見 [scene-standard.md](scene-standard.md)；素材產線見 [svg-assets.md](svg-assets.md)；自動擴展見 [expansion.md](expansion.md)。

---

## 進度總覽（2026-09-22）

**已完成（M1 原型，範圍比原訂大）**

- 4 個手畫場景、132 個物品，合在一張 2D 小鎮地圖上（3/4 俯視地面 + 正面直立物件）；可平移、縮放，左下角有小地圖。
- **自動擴展**（2026-09-22）：miko-ws 規劃場景與單字表、生成 SVG，la-game 自動整合、測試、commit，由 miko-ws runtime 每 30 分鐘推進一步。第 5 個場景「車站」（67 個物品）已自動加入；「餐廳」（70 個）生成中。詳見 [expansion.md](expansion.md)。
- 4 種關卡 + 自由探索；提示、星級、場景與關卡解鎖。
- 英語 / 日語發音：本機 edge-tts 預先合成，載入失敗時改用瀏覽器語音合成（下一步改走 miko-ws 語音生成，見 §3.5.1）。
- 詞彙本：篩選、重聽發音、顯示所在區域。
- 物件自然擺放（成群、歪倒、放上檯面），物件與背景有輕微動態；系統開啟「減少動態」時全部停止。
- 內容管線：manifest → SVG → 擺放 → 場景 JSON → 音檔，build 時檢查遮擋、區域、id 重複。
- 自動化測試：Vitest 111 個（全部通過）、Playwright E2E 6 個流程。

**還沒做**

- **試玩（0 人）**：M1 要回答的 4 個問題都還沒有答案。
- 每日複習（間隔重複）。
- 物件音效、環境音、回饋音、背景音樂。
- 動物與人物（16 個，要走 Lane A）。
- 詞彙本「在地圖上顯示位置」（目前只顯示區域名稱）。
- 發音改用 miko-ws 語音生成（ChatGPT 朗讀的自然人聲，§3.5.1）。
- PWA、Capacitor 打包。
- 手機實機效能測試。

---

## 0. 一句話概念

玩家在一座由多個場景組成的「記憶小鎮」裡找東西。每個物品都固定在同一個位置，點擊任何物品都會顯示名稱並播放發音——用**空間位置記住單字**（記憶宮殿法），用**找東西的樂趣**推動重複練習。

---

## 1. 立項（Concept / Greenlight）

### 1.1 遊戲概念

| 項目 | 內容 |
| --- | --- |
| 類型 | 2D 隱藏物件（Hidden Object）＋ 語言學習 |
| 學習語言 | 英語 + 日語；同一場景對應多種語言（物件圖不含文字，單字與音檔按語言切換） |
| 目標用戶 | 語言初學者（A1–A2），成人與青少年自學者為主，親子為輔 ⚠️ 假設 |
| 平台 | Web（PWA）先行 → 打包上架 iOS / Android |
| 核心體驗 | 沉浸在生活場景中，邊找邊聽，自然記住「什麼東西在哪裡、叫什麼」 |
| 單局時長 | 一個階段 3–5 分鐘，適合碎片時間 |

### 1.2 核心玩法循環

```
進入小鎮 → 選目前街區與關卡 → 看任務清單（本階段要找的 N 個物品）
   → 探索地圖（拖曳 / 縮放 / 小地圖）
   → 點擊任意物品：顯示名稱 + 播放發音（物件音效未做）
   → 點中目標：收集 ✓
   → 集滿清單：階段完成 → 解鎖下一階段 / 下一個街區
   → 點過的單字進入詞彙本 → 隔天回來複習（間隔重複，未做）
```

關鍵設計：**點錯不懲罰**。點到非目標物品同樣會顯示名稱和發音——「找錯」本身也是學習。

### 1.3 市場與競品

| 競品 | 類型 | 可借鏡 | 差異 |
| --- | --- | --- | --- |
| Duolingo | 遊戲化語言學習 | 連續打卡、短時長 | 無空間情境，詞彙脫離場景 |
| Drops | 圖像詞彙 | 以圖代譯、5 分鐘一局 | 無探索與空間記憶 |
| Influent（Steam） | 3D 房間點物學詞 | 「點物件聽發音」概念最接近 | 3D、單一房間、PC 限定 |
| Noun Town（VR） | VR 城鎮語言學習 | 城鎮 + 物件互動 | 需要 VR 裝置，門檻高 |
| Hidden Folks | 手繪隱藏物件 | 找物樂趣、美術風格、點擊回饋音效 | 非語言學習 |

> 市場規模、下載量、Steam 評論數等數據：⚠️ 待調研補充。

### 1.4 競爭優勢

1. **記憶宮殿結構**：物品位置固定，所有場景是同一張小鎮地圖上的街區，空間記憶與詞彙綁定。
2. **階段遞進**：同一場景從「看圖找」→「看字找」→「聽音找」→「憑記憶找」，重複遊玩有新挑戰。
3. **輕量平台**：2D + 手機 / Web，比 3D / VR 競品門檻低。
4. **內容可擴展**：新場景 = 新詞彙包，資料驅動；新場景用街區模板自動產生地形，規劃、素材、整合全自動（[expansion.md](expansion.md)）。

### 1.5 技術可行性

| 項目 | 方案（現況） | 風險 |
| --- | --- | --- |
| 場景渲染 | React + SVG：一張小鎮地圖（地形在 `WorldBackground.tsx`）+ 每個物件一個內嵌 SVG | 桌機實測閒置 / 平移都 ≥ 89 fps（CPU 降速 4 倍）；**手機實機未測** |
| 點擊判定 | 物件 SVG 內嵌，形狀就是點擊範圍；SVG 先經白名單清洗 | 小物品誤觸 → 最小點擊尺寸規範（尚未訂） |
| 擺放 | seed 自動擺放 → 鎖定在 `content/layouts/`，可手改；build 時檢查可見比例 ≥ 60%、底線不超出區域 | 上線後改位置會打亂玩家記住的位置 |
| 物件美術 | miko-ws Lane B 已生成 132/132 個 SVG，見 [svg-assets.md](svg-assets.md) | 動物 / 人物 Lane B 畫不了，要走 Lane A，兩種畫風需統一 |
| 發音 | 現況：本機 edge-tts 預先合成（en-US-JennyNeural、ja-JP-NanamiNeural，語速 −10%）。下一步：miko-ws 語音生成（ChatGPT 朗讀的自然人聲，失敗退回 edge-tts），見 §3.5.1 | 讀音未經母語者檢查（日語漢字讀法）；不同 ChatGPT 帳號的聲音可能不一致 |
| 存檔 | localStorage，讀取時逐欄驗證；之後加帳號 + 雲端同步 | 跨裝置進度、清除瀏覽器資料會遺失 |
| 框架 | Next.js 16 + React 19 + TypeScript + Tailwind v4；PWA 與 Capacitor 尚未設定 | 商店審核對 WebView 應用的要求 |

結論：無高風險技術難點，主要成本在**美術與配音內容**；目前最大的未知是手機效能。

### 1.6 成本預估（人月，⚠️ 假設）

| 角色 | 配置 | 投入 |
| --- | --- | --- |
| PM / 策劃 | 1 人 | 約 7 人月 |
| 前端程式 | 1 人 | 約 6 人月 |
| 美術 | miko-ws 生成 + 人工修圖 | 132 個物件 SVG 已生成；動物 / 人物 16 個另計 |
| 配音 | miko-ws 語音生成；核心詞是否仍外包真人錄音待定（§8） | 每場景約 70 詞 × 2 語言，場景持續自動增加 |
| 其他 | — | 開發者帳號、主機、測試招募、宣發 |

金額待外包報價後填入。

### 1.7 商業模式與回本模型

**推薦：免費試玩 + 買斷解鎖**。第 1 個場景（公園）免費，完整版或場景包付費解鎖。理由：內容量有限時，買斷比訂閱容易讓用戶接受；之後有穩定更新節奏再評估訂閱。
次要收入：學校 / 補習班授權（B2B）。

```
收入       = 下載量 × 付費轉化率 × 客單價 × (1 − 平台抽成)
回本下載量 = 總成本 ÷ (付費轉化率 × 客單價 × (1 − 平台抽成))
```

示例（⚠️ 全為假設值）：總成本 US$50,000、轉化率 3%、客單價 US$4.99、抽成 30%
→ 每個下載貢獻約 US$0.105 → 回本需約 48 萬下載。
→ 說明純 C 端買斷回本壓力大，**B2B 授權與控制美術成本**是回本的關鍵變數。

### 1.8 Go / No-Go 判斷標準（建議）

- 原型測試中 ≥ 70% 測試者不經說明即可完成階段 1，且表示想玩下一階段。
- 隔天詞彙回憶測試（看圖說出單字）正確率 ≥ 50%。
- 單個場景的美術 + 配音成本在預算內。

目前三項都還沒有數據。

### 1.9 PM 關注點

- A：資源是否到位？時程是否可行？「找東西 + 學單字」是否真的好玩，而不只是有用？
- T：定義原型要驗證的問題；組織試玩並收集回饋。

---

## 2. 前期製作（Pre-production）

### 2.1 里程碑 1：原型（約 3–4 週）

**原訂範圍**：1 個場景（公園）、占位圖、TTS 發音、階段 1–2、點擊顯示名稱 + 發音。

**實際（2026-09-22）**：4 個場景、正式 SVG、階段 1–4 全部、詞彙本、小鎮地圖都做了；**試玩還沒辦**，所以下面 4 個問題都沒有答案。

**要回答的問題**：

1. 點擊 → 名稱 + 發音的回饋是否清楚、有趣？
2. 找東西的難度是否合適（太簡單無聊、太難挫折）？
3. 手機上拖曳 / 縮放 / 點小物品的操作是否順手？
4. 玩完後是否真的記得單字？

**產出**：可玩原型 ✅、試玩紀錄（5–10 人）❌、玩法調整清單 ❌。

### 2.2 里程碑 2：垂直切片（約 6 週）

**範圍**：公園場景完整美術、真人發音、階段 1–4 全部、詞彙本、基本音效與背景音樂。

- 已提前完成：階段 1–4、詞彙本。
- 還要做：公園補上動物 / 人物並統一畫風、真人發音、音效與背景音樂、手機實機測試。

**通過標準**：外部試玩者能完整玩完一個場景，美術風格定稿，單場景製作流程（SVG 生成 → 場景擺放 → 配音 → 設定表）跑通並測出工時。
流程已寫成 [scene-standard.md](scene-standard.md) §4，但還沒有照流程從頭做一個新場景，工時未測。

### 2.3 美術方向

- 物件是正面平視的扁平向量風格（SVG），由 miko-ws 批量生成；地面是 3/4 俯視的小鎮地圖，物件直立站在地圖上（和 RPG 地圖一樣），可上下左右平移縮放。
- 物件本身不用俯視：路燈、紅綠燈、腳踏車等從正上方看認不出來，不利學單字。
- 物品需要「找得到但不能一眼看到」：靠成群擺放、互相遮擋、尺寸差異、散落小物歪倒製造難度；每個物件至少露出 60%。
- 物件圖與背景都不放任何文字或數字，確保同一場景可以用於各種語言。
- 背景不畫長得像單字物品的東西（鳥、樹、長椅、欄杆等），改用綠籬、草叢、花台、鋪面，避免玩家點了沒反應。
- 動態：會動的物件不超過場景物件的 30%，每個區域的背景也要有動態，避免「會動的就能點」變成線索。
- 動物與人物 Lane B 畫不了，原型先不放，之後走 Lane A（PNG）再統一畫風。

### 2.4 團隊配置

PM / 策劃 1、前端 1、插畫外包 1–2、配音外包；QA 由團隊內部 + 封測玩家承擔。

---

## 3. 正式開發（Production）— GDD 核心

### 3.1 場景設計

四個場景是同一張小鎮地圖上的街區，靠道路、河流、綠籬銜接（地圖配置見 [scene-standard.md](scene-standard.md) §1）。玩家沿路線解鎖：

```
公園 → 商業街 → 河邊商業區 → 超市
```

- **解鎖規則**：前一個場景過了階段 1，才開下一個場景；同一場景的階段依序解鎖。未解鎖的街區蓋一層霧，點了會提示解鎖條件。
- **物品數**：原定每場景 40–60 個，目前 30–35 個（暫緩的動物 / 人物 16 個不含在內），見第 8 節。
- 每個場景劃分 4 個區域，形成記憶宮殿的「路線」。

| 場景 | 物品數 | 區域（路線） | 物品示例 |
| --- | --- | --- | --- |
| 公園 | 33 | 入口 → 噴泉廣場 → 遊樂場 → 湖邊 | 長椅、噴泉、溜滑梯、鞦韆、風箏、路燈、野餐墊、小船 |
| 商業街 | 34 | 路口 → 商店 → 咖啡店 → 公車站 | 紅綠燈、斑馬線、郵筒、自動販賣機、消防栓、馬克杯、公車 |
| 河邊商業區 | 30 | 橋 → 碼頭 → 露天座位 → 河岸步道 | 橋、遊覽船、欄杆、燈塔、釣竿、腳踏車、救生圈、菜單 |
| 超市 | 35 | 入口 → 生鮮區 → 冷藏區 → 收銀台 | 購物車、蘋果、香蕉、牛奶、雞蛋、麵包、價格標籤、收銀機 |

### 3.2 階段設計

每個場景 4 個階段，物品位置不變，提示逐步減少（已實作，規則在 `src/lib/stages.ts`）：

| 階段 | 名稱 | 提示方式 | 目標數 | 找的方式 | 學習目的 |
| --- | --- | --- | --- | --- | --- |
| 1 | 看圖找 | 圖示 + 文字 + 發音 | 5 | 清單，任意順序 | 認識物品 |
| 2 | 看字找 | 文字 + 發音 | 8 | 清單，任意順序 | 字形連結物品 |
| 3 | 聽音找 | 只有發音 | 10 | 一次一個 | 聽力辨識 |
| 4 | 記憶挑戰 | 物品全部隱形，聽到單字後點出它**原本的位置** | 10 | 一次一個 | 記憶宮殿式回憶 |

- 記憶挑戰的判定：點在物品外框外擴至少 40 單位（或物品短邊的 25%）以內就算對；同一個物品點錯 3 次直接揭曉位置，算完成但扣星。
- **自由探索**：不設目標，任何已解鎖街區的物品都能點。

進階（上線後）：句子找物，例如「長椅下面的貓」，帶入方位詞與短句。

### 3.3 互動規則

- **點擊任何物品**：彈出名稱（英語或日語）＋ 播放發音。可在首頁切換「中文翻譯」「假名讀音」（日語）的顯示。物件音效（如噴泉水聲）還沒做。
- **點中目標**：收集動畫，清單打勾。
- **提示**：每階段 3 次；使用後鏡頭移到目標所在區域並讓區域閃爍，聽音 / 記憶關卡會再唸一次單字。
- **無時間限制、無失敗**。
- **星級只看求助程度**：提示次數 + 被揭曉的物品數 = 0 → 3 星，≤ 2 → 2 星，其餘 1 星。完成時間只記最佳紀錄，不影響星級（原規劃把時間算進星級，實作時拿掉，避免時間壓力）。

### 3.4 詞彙與複習系統

- **詞彙本**（已做）：點過的物品自動加入，沒遇過的顯示問號；可篩選「全部 / 認識的 / 找到過的」、重聽發音、看所在區域與找到次數。
  - 未做：點單字後移到地圖上的位置。
- **每日複習**（未做）：依間隔重複排程，選出待複習單字，生成一局「聽音找」或「記憶挑戰」。
  - 現有單字紀錄只有 `seen / found / lastAt`。做間隔重複要擴充存檔格式；`parseProgress` 遇到不認得的 `version` 會重設進度，所以升版時要寫轉換，不能只改版號。

### 3.5 聲音

| 類型 | 內容 | 狀態 |
| --- | --- | --- |
| 發音（必須） | 每個物品的英語、日語發音各一份 | edge-tts 版已完成（手畫 4 場景 + 車站）；改用 miko-ws 語音生成待做（§3.5.1） |
| 物件音效 | 有聲音的物品（交通工具、噴泉、動物等）點擊時播放 | 未做 |
| 環境音 | 每個場景獨立環境音（公園鳥叫、超市廣播、河邊水聲） | 未做 |
| 回饋音 | 收集、階段完成、提示 | 未做 |

#### 3.5.1 發音生成

**la-game 現況**：`npm run content:audio`（`scripts/build-audio.mjs`）在本機呼叫 edge-tts。英語用 en-US-JennyNeural 念 `en`，日語用 ja-JP-NanamiNeural 念 `ja.text`，語速都是 −10%，輸出 `public/audio/<en|ja>/<scene>/<itemId>.mp3`。已經有的檔案不重做（`--force` 才全部重做）。遊戲裡音檔載入失敗時，改用瀏覽器語音合成。

**miko-ws 最新的語音生成**（LLM 指揮中心 `POST /api/llm/voice`，參數 `text`、`outputPath`、`engine`、`instruction`、`fallback`）：

1. **預設 `engine: 'gpt'`**：讓已登入的 ChatGPT（Chrome CDP）朗讀，得到自然的人聲，不像合成音在「念稿」。
   - 先請 ChatGPT「一字一句照原稿輸出」，再機械比對輸出和原稿；被改寫或摘要就不朗讀，避免生出內容不對的音檔。
   - 按下「大聲朗讀」後，攔截播放用的 MediaSource 資料，直接拿到 AAC 音訊。不是錄螢幕、也不用虛擬音效卡，所以沒有雜音或漏字，而且比實際播放快（實測 94 秒的音檔 15 秒拿到）。
   - 逾時：原稿回應 180 秒、第一段聲音 30 秒、朗讀完成 300 秒。
2. **備援由指揮中心決定**：依序試有語音能力的 ChatGPT 帳號（chatgpt1–4，付費帳號優先），忙碌、確認已掛、冷卻中的帳號跳過；全部失敗才退回 edge-tts 合成音，確保一定產出音檔。
3. `engine: 'edge'` 直接用 edge-tts。帶 `instruction`（請 ChatGPT 依指示改寫再念）時預設不退回 edge-tts，避免把指示文字直接念出來。
4. 送出的文字會傳到 ChatGPT（外部服務）。單字沒有個資，可以送。

**la-game 改用它要做的事**（待做）：

- `build-audio.mjs` 改呼叫 miko-ws `/api/llm/voice`（和 SVG、規劃一樣經過 `scripts/lib/miko.mjs`），自動擴展的整合步驟跟著改用。
- 格式：指揮中心輸出 `.wav` 時會用 ffmpeg 轉檔，其他副檔名直接寫原始資料（AAC）。遊戲目前讀 `.mp3`，要決定轉成 mp3 還是改用 `.m4a`（還沒驗證）。
- 日語讀音：ChatGPT 念 `ja.text`（漢字）時，讀法可能和 `reading` 不同，要抽查；讀錯的改念假名。
- 聲音一致：不同 ChatGPT 帳號的朗讀聲音可能不同，要固定各帳號的語音設定，或抽查同一場景的音檔。
- 很短的單字（一兩個字）朗讀是否穩定、原稿比對會不會誤判，還沒測過。
- 速度：每個場景約 140 個音檔（70 詞 × 2 語言），每個都要排一次 ChatGPT，比 edge-tts 慢很多，適合放在自動擴展的背景流程裡跑。

### 3.6 技術設計摘要（TDD）

內容以資料驅動。手畫的前 4 個場景地形在 `WorldBackground.tsx`；之後的場景由自動擴展產生（規劃 `content/plans/<scene>.json` → manifest → 街區模板地形，見 [expansion.md](expansion.md)）。管線：

```
content/svg-manifests/<scene>.json   單字（中/英/日+讀音）、物品描述 ← 唯一來源
        │ miko-ws 生成 SVG → npm run content:sync-svg
public/svg/<scene>/<itemId>.svg
content/scene-config.json            區域（地圖矩形）、地帶、成群、散落、動態
        │ npm run content:layout -- <scene>
content/layouts/<scene>.json         鎖定的位置（可手改）
        │ npm run content:scenes（清洗 SVG + 檢查擺放 / 動態 / id 重複）
src/data/scenes/<scene>.json         遊戲讀的資料（產出物，不要手改）
        │ npm run content:audio（edge-tts）
public/audio/<en|ja>/<scene>/<itemId>.mp3
```

場景 JSON 裡每個物品的欄位：

```json
{
  "id": "bench", "zone": "entrance", "category": "...",
  "words": { "zh-TW": "長椅", "en": "bench", "ja": { "text": "ベンチ", "reading": "ベンチ" } },
  "viewBox": [0, 0, 200, 120], "body": "<清洗過的 SVG 內文>",
  "x": 120, "y": 410, "w": 180, "h": 108, "rotate": 0, "flip": false,
  "motion": { "type": "sway", "dur": 4, "delay": -1.2, "amp": 1 }
}
```

- 座標都是整張地圖的座標；音檔路徑由場景 id + 物品 id 推出來，不寫在 JSON。
- 關卡定義在程式（`src/lib/stages.ts`），所有場景共用，不寫在場景 JSON。
- 原規劃的「場景擺放工具」改成 seed 自動擺放 + 手改鎖定檔 + 預覽圖（`npm run content:scene-preview`），不另做拖放工具。
- 物件音效（`sfx`）欄位還沒有。

### 3.7 里程碑 3：Alpha（約 8–10 週）

- 4 個場景、每場景 4 階段全部可玩。——已完成
- 詞彙本、存檔可用。——已完成（存檔只有本機）
- 每日複習。——未做
- 從首次開啟到完成全部場景的流程可以完整跑通。——E2E 只測到解鎖第 2 個場景，全流程未測

---

## 4. 測試（Testing / QA）

| 測試 | 對象 | 重點 | 狀態 |
| --- | --- | --- | --- |
| 自動化測試 | — | Vitest：關卡規則、存檔解析、幾何、小鎮、擺放、動態、build；Playwright：首頁解鎖、地圖切換、自由探索、看圖找、點錯與提示、記憶挑戰 | 79 個單元測試通過；E2E 6 個流程 |
| 內部測試 | 團隊 | 流程跑通、點擊判定準確、音檔缺失 | 未做 |
| 封閉測試 | 語言學習者社群、語言老師 | 難度、學習效果、是否想每天回來 | 未做 |
| QA | 內部 | 多尺寸手機 / 平板相容、觸控誤判、音訊播放、存檔回歸、手機效能 | 未做 |

**核心指標**：

- 階段完成率、平均完成時間、提示使用率。
- D1 / D7 留存。
- 詞彙回憶率（玩後隔天測驗）——判斷「真的有學到」的關鍵指標。

目前沒有任何數據收集（無後端、無分析工具）；試玩時要人工記錄，或先加上本機匯出。

---

## 5. 上線與持續運營

### 5.1 里程碑 4：正式版本（約 2 週）

- 商店素材：截圖、宣傳短片（錄製「點擊 → 發音」的畫面）、說明文字。
- 隱私政策（有兒童用戶時需符合各平台兒童隱私規範）。
- 付費解鎖、商店提審。
- 上線前把各場景 `content/layouts/<scene>.json` 定稿；上線後不再重排（玩家記住的位置會變）。

### 5.2 運營內容

- **新場景包**：車站、餐廳、學校、醫院、機場。每個新場景要在小鎮地圖上找位置並接上現有道路 / 河流（必要時加大地圖）。
- **季節活動**：節日限定場景（聖誕市集、夏日祭）。
- **新語言**：同一套場景換配音與文字即可擴展語言。
- **社群回饋**：收集玩家想要的場景與單字。

---

## 6. 時程總表（初估）

| 里程碑 | 核心問題 | 時長 | 產出 | 狀態（2026-09-22） |
| --- | --- | --- | --- | --- |
| 立項 | 值得做嗎？ | 2 週 | 本規劃書、競品調研、立項決策 | 規劃書 v0.2；競品數據未調研 |
| M1 原型 | 找物學詞好玩嗎？ | 3–4 週 | 1 場景原型、試玩紀錄 | 原型完成（4 場景）；試玩未做 |
| M2 垂直切片 | 能做出完整一個場景的品質嗎？ | 6 週 | 公園完整版、美術定稿、單場景工時 | 部分提前完成（階段、詞彙本） |
| M3 Alpha | 核心流程能跑通嗎？ | 8–10 週 | 4 場景、詞彙本、複習、存檔 | 缺每日複習 |
| Beta / 封測 | 玩家會留下來、真的學到嗎？ | 4 週 | 測試報告、修復版本 | 未開始 |
| M4 上線 | 能按計畫發布嗎？ | 2 週 | 正式包體、商店素材 | 未開始 |

合計約 6–7 個月（原估，尚未依實際進度重排）。

---

## 7. 主要風險

| 風險 | 影響 | 對策 |
| --- | --- | --- |
| 找物好玩但學不到（或反之） | 核心價值不成立 | 原型階段就測隔天回憶率——**試玩還沒辦** |
| 美術 / 配音成本超支 | 回本困難 | M2 測出單場景工時後再排 Alpha 範圍 |
| 手機上小物品難點擊 | 挫折感 | 最小點擊尺寸規範、縮放、提示系統 |
| 手機效能未實測 | 動畫卡頓、耗電 | 真機測試；開「減少動態」可全部停止；畫面外的街區已暫停動畫 |
| 每場景物品數低於目標 | 可學單字量、找物難度不足 | 補動物 / 人物或新物件；或下修目標（第 8 節） |
| 會動的物件變成「可點」線索 | 找物變簡單 | 會動物件 ≤ 30%，每個區域背景也有動態 |
| 物件位置上線後不能改 | 美術重生、擺放調整受限 | 上線前定稿擺放；build 時檢查遮擋與區域 |
| TTS 讀音錯誤 | 學到錯的發音 | 母語者檢查一輪；核心詞改真人錄音 |
| 重複遊玩膩 | 留存低 | 四階段遞進 + 每日複習 + 新場景包 |
| 多語言擴展成本 | 內容量倍增 | 物件圖不含文字；單字與音檔按語言獨立存放 |

---

## 8. 待確認

1. ~~目標學習語言~~ → 已定：英語 + 日語。UI 母語是否只做繁中？
2. **目標用戶**：成人自學者為主，還是兒童 / 親子為主？（影響美術風格、隱私合規、商業模式）
3. **平台優先順序**：Web 先行是否可接受？
4. **預算與團隊**：是否有插畫師，還是全部外包？
5. **每場景物品數**：原定 40–60，目前 30–35。要補物件，還是下修目標？
6. **M1 試玩**：何時辦、找誰（原定 5–10 人）？要不要先加試玩數據的本機匯出？
7. **發音**：改用 ChatGPT 朗讀（§3.5.1）後，核心詞還要不要外包母語者錄音？建議先抽查一個場景的品質再決定。

---

## 版本紀錄

- **v0.1（2026-09-21）**：草案。同日完成 M1 原型：4 個場景共 132 個物品、4 種關卡 + 自由探索、英日發音、詞彙本。
- **v0.2（2026-09-22）**：4 個場景合成一張可平移、縮放的 2D 小鎮地圖；物件自然散放、加入輕微動態（[scene-standard.md](scene-standard.md)）。規劃書依實作更新：進度總覽、技術現況、關卡與星級規則、TDD 資料格式、各里程碑狀態、新增風險與待確認項目。
- **v0.3（2026-09-22）**：新場景改為自動擴展（miko-ws 規劃與生成、每 30 分鐘推進一步），第 5 個場景「車站」已自動加入；發音生成寫入 miko-ws 最新做法（ChatGPT 朗讀的自然人聲，備援 edge-tts）與 la-game 改用它的待辦（§3.5.1）。
